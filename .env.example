# Database Configuration
# Use the pooled connection URL from Supabase or your PostgreSQL provider
DATABASE_URL=postgresql://user:password@host:port/database

# Alternative database URL (fallback)
# POSTGRES_URL=postgresql://user:password@host:port/database

# Backend API Configuration
# Set this to the full URL of your backend API server (e.g., https://api.example.com)
# If not set, API requests will use relative paths (calls to the same Next.js server)
# NEXT_PUBLIC_API_URL=https://api.example.com

# Server-side only API URL (alternative to NEXT_PUBLIC_API_URL)
# Use this if you want different API URLs for server-side vs client-side
# API_BASE_URL=http://internal-api.example.com

# Amello API Configuration
# Base URL for the Amello API (used for processing scans)
AMELLO_BASE_URL=https://prod-api.amello.plusline.net/api/v1
